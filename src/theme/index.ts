import * as colors from './colors';
import * as metrics from './metrics';
import * as fonts from './fonts';
import * as animations from './animations';

export default {...colors, ...metrics, ...fonts, ...animations};
